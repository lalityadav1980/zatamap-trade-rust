# Server
SERVER_ADDR=127.0.0.1:8080

# Postgres (pick one approach)
DATABASE_URL=host=localhost port=5432 dbname=zatamap_trade user=zatamap password=your_password sslmode=disable
# Or use PG* like the Python project:
# PGHOST=localhost
# PGPORT=5432
# PGDATABASE=zatamap_trade
# PGUSER=zatamap
# PGPASSWORD=your_password
# PGSSLMODE=disable

# Kite
KITE_CALLBACK_URL=http://127.0.0.1:8080/api/kite/callback

# Optional: check token presence on startup
# AUTOLOGIN_USER_ID=some_userid

# Optional (for CLI commands: profile/holdings)
# KITE_API_KEY=your_api_key
# KITE_ACCESS_TOKEN=your_access_token
